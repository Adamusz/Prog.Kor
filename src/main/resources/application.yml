server:
  port: ${PORT:8080}

spring:
  jpa:
    hibernate.ddl-auto: update
  datasource:
    url: jdbc:h2:mem:quizegame
    username: sa
    password:
    h2:
      console:
        enabled: true
        path: /h2-console
